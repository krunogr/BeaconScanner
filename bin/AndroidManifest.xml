<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.radiusnetworks.ibeaconreference"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="18"
        android:targetSdkVersion="18" />

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.radiusnetworks.ibeaconreference.MainActivity"
            android:label="@string/app_name"
            android:launchMode="singleInstance" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.radiusnetworks.ibeaconreference.RangingActivity"
            android:label="@string/app_name" >
            <intent-filter>
            </intent-filter>
        </activity>
        <activity
            android:name="com.radiusnetworks.ibeaconreference.ScanActivity"
            android:label="@string/app_name"
            android:launchMode="singleInstance" >
            <intent-filter>
            </intent-filter>
        </activity>
        <activity
            android:name="com.radiusnetworks.ibeaconreference.BackgroundActivity"
            android:label="@string/app_name" >
            <intent-filter>
            </intent-filter>
        </activity>
        <activity
            android:name="com.radiusnetworks.ibeaconreference.AllFilesBeaconActivity"
            android:label="@string/title_activity_all_files_beacon" >
        </activity>
        <activity
            android:name="com.radiusnetworks.ibeaconreference.ContactActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="com.radiusnetworks.ibeaconreference.PhoneActivity"
            android:label="@string/title_activity_telephone" >
        </activity>
    </application>

</manifest>